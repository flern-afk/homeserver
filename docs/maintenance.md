# Wartung

## Container

Laufende Container:

```bash
docker ps
```

Logs:

```bash
docker logs <container>
```

Container stoppen:

```bash
docker stop <container>
```

Container starten:

```bash
docker start <container>
```

Updates:

```bash
docker compose pull
docker compose up -d
```

